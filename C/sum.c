#include <stdio.h>

int sum()
{
    int S=0, i;
    for (i=0;i<4;i++)
    {
        S = S + i
    }
    return (S);
}

int main()
{
        int sum;
        Sum = sum();
        printf("Sum = %d \n",Sum);
}